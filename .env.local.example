# Docker Build Configuration
# =============================
# File ini menentukan mode build untuk Docker
# Copy file ini ke .env.local dan sesuaikan dengan server Anda
#
# File .env.local sudah di-gitignore, jadi aman dari overwrite saat pull/push

# BUILD_MODE menentukan environment file mana yang akan dibaca:
# - development: akan membaca .env.development (server 226)
# - production: akan membaca .env.production (server 223)

# Untuk Development Server (226):
# BUILD_MODE=development

# Untuk Production Server (223):
# BUILD_MODE=production

# Default (uncomment salah satu):
BUILD_MODE=production
